
Fix speichern button and complete inventory
24. Juli
Felixbads/Wawi
main

Archivieren

Weitergeben

Bei der Inventur: Der Button "jetzt speichern" führt immer noch zu einem Fehler und der Button Inventur abschließen erscheint nicht.
Kopieren
### Issue 1: “Jetzt speichern” always fails (404) The button triggers `fetch("/api/inventory-counts/${id}/save")` inside `client/src/pages/InventurDetailNewPage.tsx`​:codex-file-citation[codex-file-citation]{line_range_start=820 line_range_end=831 path=client/src/pages/InventurDetailNewPage.tsx git_url="https://github.com/Felixbads/Wawi/blob/main/client/src/pages/InventurDetailNewPage.tsx#L820-L831"}​. However, `server/routes.ts` exposes most inventory endpoints at `/api/inventory-counts` but **does not** implement `/api/inventory-counts/:id/save`​:codex-file-citation[codex-file-citation]{line_range_start=920 line_range_end=1120 path=server/routes.ts git_url="https://github.com/Felixbads/Wawi/blob/main/server/routes.ts#L920-L1120"}​. A matching save route exists only in `server/routes/inventory.ts`, which is mounted at `/api/inventory`​:codex-file-citation[codex-file-citation]{line_range_start=5168 line_range_end=5173 path=server/routes.ts git_url="https://github.com/Felixbads/Wawi/blob/main/server/routes.ts#L5168-L5173"}​, so its path becomes `/api/inventory/inventory-counts/:id/save`​:codex-file-citation[codex-file-citation]{line_range_start=432 line_range_end=469 path=server/routes/inventory.ts git_url="https://github.com/Felixbads/Wawi/blob/main/server/routes/inventory.ts#L432-L469"}​. This mismatch leads to “Not Found” responses when “Jetzt speichern” is clicked. :::task-stub{title="Add /api/inventory-counts/:id/save endpoint"} 1. In `server/routes.ts`, after the block handling `POST /inventory-counts/:id/items`, replicate the save logic from `server/routes/inventory.ts`: - Parse `inventoryCountId`. - Verify the inventory count exists. - Update `updatedAt` and optional `notes`. - Respond with JSON `{ message, inventoryCountId, warehouseName, savedAt }`. 2. Ensure route path is `app.post(\`${API_PREFIX}/inventory-counts/:id/save\`, …)`. 3. Keep existing route in `server/routes/inventory.ts` (or consolidate later) so both prefixes work. 4. Commit with a message like “add inventory count save endpoint”. This enables the front end’s existing requests to succeed. ::: ### Issue 2: Notes payload mismatch `performAutoSave` sends an array of note objects: ```ts body: JSON.stringify({ notes: Object.entries(editedNotes).map(([itemId, note]) => ({ itemId: parseInt(itemId), note })) }) ``` ​:codex-file-citation[codex-file-citation]{line_range_start=824 line_range_end=831 path=client/src/pages/InventurDetailNewPage.tsx git_url="https://github.com/Felixbads/Wawi/blob/main/client/src/pages/InventurDetailNewPage.tsx#L824-L831"}​ `server/routes/inventory.ts` expects `notes` to be a simple string and writes it directly into the `inventory_counts.notes` column​:codex-file-citation[codex-file-citation]{line_range_start=447 line_range_end=463 path=server/routes/inventory.ts git_url="https://github.com/Felixbads/Wawi/blob/main/server/routes/inventory.ts#L447-L463"}​. Submitting an array may produce invalid text or errors. :::task-stub{title="Send inventory note string during save"} 1. Update `performAutoSave` in `InventurDetailNewPage.tsx`: - Send a plain string (e.g., `notes: completionNotes`) or omit the field if unused. - Ensure edited item notes are saved via existing item update mutations only. 2. Adjust any related types or usages. 3. Commit with a message like “align save request with backend notes format”. ::: These fixes will allow “Jetzt speichern” to succeed and ensure inventory completion buttons appear once statuses update correctly.

