### Ursache * **Batches werden ausgeblendet** Die Tabelle gruppiert alle Chargen (`allBatches`) und filtert dabei Chargen mit `currentQuantity > 0` heraus. Existieren für Lager 10 zwar Chargen, deren Bestandsmenge jedoch 0 ist, erscheinen sie nicht in der Oberfläche​:codex-file-citation[codex-file-citation]{line_range_start=108 line_range_end=111 path=client/src/components/inventory/WarehouseInventoryTable.tsx git_url="https://github.com/Felixbads/Wawi/blob/main/client/src/components/inventory/WarehouseInventoryTable.tsx#L108-L111"}​ * **Warenbewegungen fehlen komplett** Der API‑Endpunkt `/api/warehouses/:id/movements` liefert eine leere Liste zurück, sobald dem Lager keine Automaten zugewiesen sind. Das geschieht bevor bereits gefundene Bewegungen (`sourceMovements`, `destMovements`) kombiniert werden, wodurch selbst vorhandene Warenein- oder -ausgänge nicht angezeigt werden​:codex-file-citation[codex-file-citation]{line_range_start=481 line_range_end=491 path=server/routes.ts git_url="https://github.com/Felixbads/Wawi/blob/main/server/routes.ts#L481-L491"}​ ### Verbesserungsbedarf * Nicht‑sichtbare Batches: optional auch Chargen mit `currentQuantity = 0` darstellen oder den Filter konfigurierbar machen. * Bewegungs-Endpunkt: frühzeitigen Abbruch entfernen bzw. Bewegungen auch ohne Automatenzuordnung zurückgeben. ### Empfohlene Aufgaben Nach jedem Problem folgt ein Task‑Stub mit konkreten Umsetzungsschritten. --- Chargen mit Bestandsmenge 0 werden komplett unterdrückt; dadurch erscheinen bei Lager 10 keine Batches, obwohl welche existieren könnten. :::task-stub{title="Null-Bestände in Batch-Liste sichtbar machen"} 1. Öffne `client/src/components/inventory/WarehouseInventoryTable.tsx`. 2. Entferne oder parameterisiere den Filter `batch.currentQuantity > 0` in der `filteredBatches`‑Berechnung. 3. Ergänze optional einen Hinweistext oder Badge für Batches mit Menge 0. 4. Teste, ob Chargen mit Menge 0 jetzt angezeigt werden. ::: --- Der Endpunkt `/api/warehouses/:id/movements` bricht ab, wenn dem Lager keine Automaten zugewiesen sind, und liefert dann immer eine leere Liste. :::task-stub{title="Warenbewegungen auch ohne Automatenzuordnung ausgeben"} 1. Bearbeite `server/routes.ts` im Abschnitt *GET /warehouses/:id/movements*. 2. Entferne die `machineIds.length === 0`‑Prüfung oder verschiebe den `return`, damit `sourceMovements` und `destMovements` stets in `combinedMovements` einfließen. 3. Stelle sicher, dass bei fehlenden Maschinen lediglich die Refill‑Abfrage übersprungen wird. 4. Führe Tests oder manuelle API‑Aufrufe durch, um die Ausgabe zu verifizieren. ::: ### Tests Keine Tests ausgeführt – reine Codeinspektion. ### Notes - Behebung erfordert Eingriffe sowohl im Frontend (Batch-Filter) als auch im Backend (Warenbewegungen-Endpunkt).