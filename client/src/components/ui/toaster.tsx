import { useToast } from "@/hooks/use-toast"
import {
  Toast,
  ToastClose,
  ToastDescription,
  ToastProvider,
  ToastTitle,
  ToastViewport,
} from "@/components/ui/toast"

export function Toaster() {
  // GLOBAL TOAST DEACTIVATION - Toaster ist global deaktiviert
  // Gibt nichts zurück, um störende Pop-ups zu vermeiden
  return null
}
